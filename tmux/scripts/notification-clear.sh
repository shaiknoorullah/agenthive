#!/usr/bin/env bash
# notification-clear.sh — clears the @notif-* tmux user options.
#
# Skeleton only. The real script (in a follow-up commit) will run:
#   tmux set -gu @notif-msg
#   tmux set -gu @notif-project
#   tmux set -gu @notif-source
#   tmux set -gu @notif-time
#   tmux set -gu @notif-priority
#
# It is invoked from the pane-focus-in hook the plugin installs.

set -eu

echo "not implemented: tmux/scripts/notification-clear.sh"
